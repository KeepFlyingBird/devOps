1、查询镜像列表
     docker images
   查找具体镜像
     docker search 镜像
2、删除镜像
    docker rmi 镜像
3、Dockerfile 构建镜像
4、设置镜像标签
    docker tag 860c279d2fec runoob/centos:dev
    docker tag 镜像ID 镜像源名(repository name)和新的标签名(tag)