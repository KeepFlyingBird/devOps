1、拉取镜像
    docker pull 镜像

2、启动镜像
    docker run --detach  \
    	--hostname localhost \
    	--publish 443:443 \
    	--publish 80:80 \
    	--publish 22:22 \
    	--name gitlab \
    	--restart always \
    	--volume /srv/gitlab/config:/etc/gitlab \
    	--volume /srv/gitlab/logs:/var/log/gitlab \
    	--volume /srv/gitlab/data:/var/opt/gitlab \
    	gitlab/gitlab-ce:latest


demo1:
    拉取gitlib镜像：docker pull gitlab/gitlab-ce:latest